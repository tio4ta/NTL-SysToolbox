Audit d'Obsolescence Réseau

Ce script Python 3 permet d’effectuer un audit complet des systèmes d’exploitation sur un réseau et de vérifier leur statut de support (EOL).

Fonctionnalités principales

🔹 Scan réseau avec Nmap pour détecter tous les hôtes actifs.

🔹 Extraction automatique des OS : Windows, Windows Server, Ubuntu, Debian.

🔹 Vérification EOL via l’API endoflife.date
.

🔹 Import de fichiers CSV existants.

🔹 Affichage clair des hôtes avec IP, Hostname, OS, Version, EOL et statut.

🔹 Génération de rapports : CSV, JSON et HTML avec badges colorés.

🔹 Liste des versions supportées pour chaque OS.

Pourquoi Nmap ?

Nmap est utilisé pour :

✅ Détecter les hôtes actifs sur un réseau.

✅ Identifier les systèmes d’exploitation et versions.

✅ Récupérer les informations SSL (commonName).

✅ Scanner sans installer d’agents sur les machines.

Remarque : Nmap offre un bon compromis vitesse / précision, et permet de récupérer des informations qu’aucun simple script ne pourrait obtenir localement.

Limites connues

Versions Linux : Nmap ne détecte pas toujours les versions exactes des distributions. Dans ce cas, le script indique :
Version non détectée par Nmap et recommande une vérification manuelle (cat /etc/os-release).

Historique et évolutions :

Gestion des différentes versions de Windows (XP → 11).

Support des serveurs Windows avec R2.

Robustesse du parsing XML Nmap.

Ajout d’un cache pour limiter les appels à l’API EOL.

Génération automatique de rapports CSV, JSON et HTML.

Installation
git clone https://github.com/ton-utilisateur/ton-repo.git
cd ton-repo
pip install -r requirements.txt


⚠️ Nmap doit être installé sur la machine pour que le script fonctionne.

Utilisation

Lancer le script :

python audit_obsolescence.py


Menu interactif :

Scanner réseau (ex: 10.5.70.0/24)

Importer CSV

Afficher hôtes détectés

Lister versions d’un OS

Générer rapport (CSV, JSON, HTML)

Quitter

Exemple de sortie
IP	Hostname / CN	OS	Version	Date EOL	Statut
10.5.70.12	srv1	Windows 10	21H2	2025-10-14	SUPPORTÉ
10.5.70.15	srv2	Ubuntu	20.04	2025-04-30	EOL < 1 AN
10.5.70.18	srv3	Debian	Unknown	N/A	Vérification manuelle requise

⚡ Les badges HTML du rapport permettent de visualiser rapidement le statut : rouge = obsolète, orange = EOL imminent, vert = supporté.

Conclusion

Ce script est un outil pratique pour les administrateurs réseau :

Identifier rapidement les systèmes obsolètes.

Générer des rapports clairs et exportables.

Anticiper les mises à jour avant la fin de support.

Il combine automatisation, précision et flexibilité grâce à Nmap et l’API EOL, tout en restant simple et léger.
